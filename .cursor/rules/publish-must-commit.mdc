---
description: 提交、推送、发布是一套连续流程，不要走一步停一步
alwaysApply: true
---

# 提交、推送、发布一次做完

用户要求整套流程自动完成，禁止走一步停一步再问“要不要推”“要不要发布”。

- 用户说「提交」= `git commit` + `git push` 到 GitHub。不要只 commit 完就停。
- 只要发到服务器，顺序是：测试通过 → commit → push → `./deploy/publish.sh`。缺一不可。
- 不要问「要不要提交」「要不要推上去」「要不要发布」。做完后直接回报 commit、远程分支、发布目标和验收。
- 这覆盖默认的“没说 push 就不要 push”。本仓库里 push 是提交的一部分。
- 三个前端的 `dist/` 不得被忽略；正式构建和发布时，把对应 `dist/` 与源码一起检查、提交并推送。
- 不要提交 `.env`、密钥、数据库、上传、缓存或日志。
- 提交或 push 失败先修好；若生产已更新而 GitHub 落后，立刻补提交补 push。
