---
description: 禁止自动切换分支，打包发布前必须推送 GitHub，所有 GitHub 改动描述使用中文
alwaysApply: true
---

# 分支与 GitHub 同步长期规则

以下规则始终适用于本仓库中的 Cursor 开发对话。

## 禁止自动切换分支

- 开始工作后保持当前分支不变。
- 不得因为修复、发布、同步、解决冲突或工具默认行为自行执行 `git switch`、`git checkout`、创建新分支、进入 detached HEAD，或改到其他 worktree。
- 任何会改变当前分支的操作，都必须先告诉用户目标分支和切换原因，等待用户明确同意后才能执行。
- 提交和发布前后都要检查当前分支，发现分支意外变化时停止操作并告知用户。

## 打包、推送与发布一次完成

- 用户要求打包、部署或发布时，必须按以下顺序连续完成：检查当前分支与改动 → 测试/构建 → 提交本次相关源码 → 推送当前分支到 GitHub → 确认远程包含该提交 → 发布服务器 → 线上验收。
- `git push` 失败时不得继续发布。
- 禁止只在本地提交，禁止生产代码领先于 GitHub，禁止打包未推送的源码作为正式发布版本。
- 只提交本次相关文件，不覆盖或夹带用户已有的无关改动；不得提交密钥、数据库、上传文件、缓存、日志或 `dist/`。

## GitHub 描述使用中文

- Git 提交标题和正文必须使用清晰中文。
- GitHub Pull Request 的标题和说明必须使用中文。
- GitHub Release 的标题和更新说明必须使用中文。
- 命令、文件名、标识符和无法准确翻译的代码术语可以保留原文。

## 完成后报告

向用户明确报告当前分支、提交号、推送到的 GitHub 远程分支，以及发布和线上验收结果。
