---
description: Admin 管理后台禁止自动切换分支，并强制完成中文提交、GitHub 推送和可追溯发布
alwaysApply: true
---

# Admin 管理后台长期规则

- 禁止自动切换或创建分支；任何分支变化必须先取得用户明确同意。
- 打包或发布必须依次完成测试、构建、检查并提交源码和 `dist/`、推送 GitHub、确认远程提交、运行仓库发布脚本和线上验收；推送失败时禁止发布。
- Git 提交、Pull Request 和 Release 的标题与说明使用中文。
- `dist/` 不得忽略；正式构建产物与对应源码一起提交和推送。
- 只处理本次相关文件并保留用户已有改动；完成后报告分支、提交、推送和验收结果。
